<!-- ---
import type { Props as $, List as List } from "astro-portabletext/types";
import { List as DefaultList } from "astro-portabletext/components";
import BulletListStyle from "./Styles/BulletListStyle.astro";
import NumberedListStyle from "./Styles/NumberedListStyle.astro";

export type Props = $<List>;

const props = Astro.props;
const listItemIs = (listItem: string) => listItem === props.node.listItem;

const Cmp = listItemIs("bullet") ? BulletListStyle 
: listItemIs('number') ? NumberedListStyle 
: DefaultList;
---

<Cmp {...props}><slot /></Cmp> -->
